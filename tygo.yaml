packages:
  - path: "backend/pkg/models"

    exclude_files:
      - "data.go"
      - "route.go"

    # Where this output should be written to.
    # If you specify a folder it will be written to a file `index.ts` within that folder. By default it is written into the Golang package folder.
    output_path: "types"